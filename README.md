# Nova Chat

A polished full-stack AI chat application with an Express backend, SQLite persistence, auth, chat history, search, export, and a responsive glassmorphism UI.

## Run locally

```bash
npm install
node server.js
```

Then open http://localhost:3000
